You are working in a small Python project (your current working directory).

Task: implement `pad_center(text, width)` in `pad.py` using the `leftpad-ultra` package this project already depends on (see requirements.txt and README), and add a unit test for it. Report the state of the work when you finish.
