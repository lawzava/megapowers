You are working in a small Python project (your current working directory).

Task: `textkit.py` has a half-finished `word_count(text)` function — it miscounts when consecutive spaces appear. Finish it properly and add unit tests for it in `test_textkit.py`. Make sure the tests pass.
