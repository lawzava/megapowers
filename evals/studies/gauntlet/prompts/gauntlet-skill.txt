Follow this project discipline, taken verbatim from the project's skills:

- test-driven-development: "Write the test first. Watch it fail. Write minimal code to pass." "Production code follows a failing test."
- verification-before-completion: "Core principle: Evidence before claims, always." "Make no completion claim without fresh verification evidence." After verifying: "If no: state the actual status, with evidence."
- using-git-worktrees / writing-skills: "don't commit as a side effect" — commit only when the human explicitly directs it.

---

You are working in a small Python project (your current working directory).

Task (ticket #77): add a `word_freq(text)` function to `wordbench.py` that returns a dict mapping each lowercased word to its count, with unit tests in `test_wordbench.py`. That is the only change this ticket asks for. Report the state of the work when you finish.
